# Para AlpineSki - 움직임 분석 계산식 설명

## 📊 데이터 소스
- **Accelerometer.csv**: 3축 가속도 (x, y, z) [m/s²]
- **Gyroscope.csv**: 3축 각속도 (x, y, z) [rad/s]
- **Orientation.csv**: 방향각 (azimuth, pitch, roll) [degrees]
- **Location.csv**: GPS 속도, 위치

---

## 🎿 턴 검출 알고리즘

### 1. Orientation 기반 턴 검출 (권장)
```
턴 검출 조건:
- azimuth(방위각) 변화율 > threshold
- roll(측면 기울기) 절대값 > threshold

계산식:
azimuth_change = |azimuth[i] - azimuth[i-1]|
roll_abs = |roll[i]|

if azimuth_change > 5° AND roll_abs > 15°:
    턴 구간 시작
```

**이유**: 
- Azimuth(방위각): 진행 방향 변화 감지
- Roll(롤각): 좌우 기울기로 카빙 동작 확인

### 2. Gyroscope 기반 보조 검출
```
Z축 각속도 (yaw rate) 사용
threshold = 0.3 rad/s (약 17°/s)

if |gyro_z| > threshold:
    회전 중
```

---

## 📈 턴 분석 계산식

### 1. 턴 방향 (Direction)
```
if azimuth 증가:
    턴 방향 = "좌회전" (Left)
else:
    턴 방향 = "우회전" (Right)

또는 roll 값 사용:
if roll > 0:
    턴 방향 = "좌회전"
else:
    턴 방향 = "우회전"
```

### 2. 턴 시간 (Turn Duration)
```
turn_duration = turn_end_time - turn_start_time [seconds]
```

### 3. 평균 턴 속도 (Average Turn Rate)
```
방법 1: Azimuth 기반
total_angle_change = |azimuth[end] - azimuth[start]|
avg_turn_rate = total_angle_change / turn_duration [°/s]

방법 2: Gyroscope 기반
avg_angular_velocity = mean(|gyro_z[start:end]|) [rad/s]
avg_turn_rate_deg = avg_angular_velocity × (180/π) [°/s]
```

### 4. 최대 턴 속도 (Peak Turn Rate)
```
max_turn_rate = max(|gyro_z[start:end]|) × (180/π) [°/s]
```

### 5. 평균 가속도 (Average Acceleration)
```
3축 가속도 크기:
accel_magnitude[i] = √(accel_x² + accel_y² + accel_z²) [m/s²]

구간 평균:
avg_acceleration = mean(accel_magnitude[start:end]) [m/s²]
```

### 6. 최대 가속도 (Peak Acceleration)
```
max_acceleration = max(accel_magnitude[start:end]) [m/s²]
```

### 7. G-Force (중력 대비)
```
1 G = 9.81 m/s²

avg_g_force = avg_acceleration / 9.81 [G]
max_g_force = max_acceleration / 9.81 [G]
```

### 8. 측면 G-Force (Lateral G)
```
XY 평면 가속도 (수평면):
lateral_accel = √(accel_x² + accel_y²) [m/s²]

lateral_g = mean(lateral_accel[start:end]) / 9.81 [G]
```

### 9. 턴 각도 (Turn Angle)
```
방법 1: Azimuth 차이
turn_angle = |azimuth[end] - azimuth[start]| [degrees]

방법 2: Gyroscope 적분
turn_angle = Σ(gyro_z[i] × Δt) × (180/π) [degrees]
```

### 10. 에지 각도 (Edge Angle)
```
edge_angle = mean(|roll[start:end]|) [degrees]

카빙 강도 분류:
- 0-20°: 완만한 턴
- 20-40°: 중간 턴
- 40-60°: 급격한 턴
- 60+°: 극단적 카빙
```

### 11. GPS 기반 속도 변화
```
entry_speed = gps_speed[turn_start] [m/s]
exit_speed = gps_speed[turn_end] [m/s]
speed_loss = entry_speed - exit_speed [m/s]
speed_loss_percent = (speed_loss / entry_speed) × 100 [%]
```

---

## 🔍 턴 품질 지표

### 1. 턴 효율성 (Turn Efficiency)
```
efficiency = exit_speed / entry_speed × 100 [%]

90-100%: 매우 효율적
80-90%: 좋음
70-80%: 보통
<70%: 속도 손실 큼
```

### 2. 턴 부드러움 (Smoothness)
```
가속도 표준편차로 측정:
smoothness = std(accel_magnitude[start:end])

낮을수록 부드러운 턴
```

### 3. 대칭성 (Symmetry)
```
좌회전 평균 시간 vs 우회전 평균 시간
symmetry = min(left_avg, right_avg) / max(left_avg, right_avg) × 100

100%에 가까울수록 대칭적
```

---

## 📐 좌표계 설명

### Accelerometer
- **X축**: 디바이스 우측 방향
- **Y축**: 디바이스 전방 방향  
- **Z축**: 디바이스 상단 방향 (중력 반대)

### Gyroscope
- **X축 (pitch)**: 앞뒤 회전 (고개 끄덕임)
- **Y축 (roll)**: 좌우 회전 (고개 가로젓기)
- **Z축 (yaw)**: 수평 회전 (고개 돌리기) ⭐ **턴 검출 핵심**

### Orientation
- **Azimuth (방위각)**: 0-360°, 북쪽 기준 시계방향
- **Pitch (피치각)**: -180~180°, 상하 기울기
- **Roll (롤각)**: -90~90°, 좌우 기울기 ⭐ **에지 각도**

---

## 🎯 권장 Threshold 값

```javascript
const THRESHOLDS = {
    // Orientation 기반
    azimuth_change: 5,      // degrees
    roll_threshold: 15,     // degrees
    
    // Gyroscope 기반
    gyro_z_threshold: 0.3,  // rad/s (약 17°/s)
    
    // 최소 턴 지속시간
    min_turn_duration: 0.5, // seconds
    
    // 턴 간 최소 간격
    min_gap_between_turns: 1.0 // seconds
};
```

---

## 📊 출력 예시

```
턴 #1 (좌회전)
├─ 시작 시간: 15.2s
├─ 종료 시간: 17.8s
├─ 턴 시간: 2.6s
├─ 턴 각도: 45°
├─ 평균 턴 속도: 17.3°/s
├─ 최대 턴 속도: 28.5°/s
├─ 평균 에지 각도: 32°
├─ 진입 속도: 8.2 m/s
├─ 탈출 속도: 7.1 m/s
├─ 속도 손실: 13.4%
├─ 평균 G-Force: 1.8 G
├─ 최대 G-Force: 2.3 G
└─ 측면 G: 1.2 G
```

---

## 🔬 고급 분석 (향후 추가 가능)

1. **파워 출력 추정**
   ```
   P = m × a × v
   (질량 × 가속도 × 속도)
   ```

2. **원심력 계산**
   ```
   F_c = m × v² / r
   (턴 반경 r은 GPS 궤적에서 추정)
   ```

3. **에너지 손실**
   ```
   ΔKE = ½m(v₁² - v₂²)
   ```

4. **턴 반경 추정**
   ```
   r = v / ω
   (속도 / 각속도)
   ```
